# docs/requirements.txt
#
# Direct dependencies for MkDocs site build.
# Pin exact versions so CI is reproducible and Renovate can track updates.
#
# To regenerate with hash-pinning (full supply-chain lock):
#   pip install pip-tools
#   pip-compile --generate-hashes docs/requirements.in --output-file docs/requirements.txt
#
# To update versions manually:
#   pip install --upgrade <package> && pip freeze | grep <package>

# ── Core ─────────────────────────────────────────────────────────────────────
mkdocs==1.6.1

# ── Theme ────────────────────────────────────────────────────────────────────
# Brings in pymdown-extensions and other transitive deps automatically,
# but we pin them explicitly so Renovate can track each one independently.
mkdocs-material==9.7.7

# Required by all pymdownx.* markdown_extensions in mkdocs.yml
pymdown-extensions==11.0.1

# ── Plugins ──────────────────────────────────────────────────────────────────
# plugins.git-revision-date-localized
mkdocs-git-revision-date-localized-plugin==1.3.0

# plugins.minify
mkdocs-minify-plugin==0.8.0

# ── Versioning ───────────────────────────────────────────────────────────────
# extra.version.provider: mike
# Used by the versioning strategy documented in docs/versioning.md
mike==2.1.3

# Security constraints for transitive dependencies.
# GitPython 3.1.59 resolves PYSEC-2026-3785, PYSEC-2026-3786,
# PYSEC-2026-3787, PYSEC-2026-3788, and PYSEC-2026-3837.
GitPython==3.1.59

# idna >= 3.15 resolves PYSEC-2026-215.
idna>=3.15
